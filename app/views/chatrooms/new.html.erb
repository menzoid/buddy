<div>
  <h3 class="mt-4">Name your new Chatroom with (otheruser)</h3>
  <div>
    <%= simple_form_for [@event, @booking] do |f| %>
      <%= f.input :name %>
      <%= f.submit %>
    <% end %>
  </div>
</div>
